# Team Marker Update

The team scoreboard marker now keeps the configured team name visible together with its configured registration command. The command is sourced from the existing command configuration and is not hard-coded into the overlay.
